import { Alert } from '@mui/material';
import React from 'react'

const AlertProvider = (type) => {

    return (
        <Alert style={{}}>
            Hola
        </Alert>
    )
}

export default AlertProvider;